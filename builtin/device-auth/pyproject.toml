[project]
name = "device-auth"
version = "1.0.0"
description = "设备 Token 的签发、列出与撤销"
authors = [{ name = "sleepy-project contributors" }]
dependencies = []

[tool.sleepy]
enabled = true
dependencies = []
